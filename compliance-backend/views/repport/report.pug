doctype html
html
  head
    title Rapport de sécurité
    style.
      body { font-family: Arial, sans-serif; }
      .header { background: #f5f5f5; padding: 20px; }
      .content { margin: 20px; }
      table { width: 100%; border-collapse: collapse; }
      th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
      th { background-color: #f2f2f2; }
  body
    .header
      h1 Rapport de conformité sécurité
      p Date: #{reportDate}
    
    .content
      h2 Résumé
      p Ce rapport présente les résultats de l'audit de conformité sécurité.
      
      h2 Détails
      table
        thead
          tr
            th Point de contrôle
            th Statut
            th Commentaires
        tbody
          each item in complianceItems
            tr
              td= item.checkpoint
              td= item.status
              td= item.comments
      
      h2 Recommandations
      ul
        each recommendation in recommendations
          li= recommendation
      
      footer
        p Généré automatiquement par le système de conformité sécurité.